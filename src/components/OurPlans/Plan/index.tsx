import { PlanAction } from './PlanAction'
import { PlanContent } from './PlanContent'
import { PlanList } from './PlanList'
import { PlanRoot } from './PlanRoot'

/**
 * Plan object containing the components for a plan.
 */
export const Plan = {
    Root: PlanRoot,
    Content: PlanContent,
    List: PlanList,
    Action: PlanAction,
}
